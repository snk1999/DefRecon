# DefRecon
A tool that identifies and visualizes the protective and delivery layers behind a web application. It detects firewalls, WAFs, load balancers, CDNs, and origin servers using DNS, TLS, headers, and error responses, outputting a clear schema with confidence scores.
